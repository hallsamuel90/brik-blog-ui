import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#01090F',
      },
      secondary: {
        main: '#f72c47',
      },
    }
  });

export default theme;